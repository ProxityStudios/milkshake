// TODO: configure in json file

export enum Core {
	Ping = 'ping',
	Info = 'info'
}

export enum Owner {
	Eval = 'eval',
	Shutdown = 'shutdown'
}

export enum Staff {
	BotInformation = 'bot-info'
}

export enum Admin {
	Language = 'language',
	Prefix = 'prefix'
}
