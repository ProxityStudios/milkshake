// TODO: configure in json file

export enum Core {
	Ping = 'ping'
}

export enum Owner {
	Eval = 'eval',
	Shutdown = 'shutdown'
}

export enum Staff {
	BotInformation = 'bot-info'
}

export enum Admin {
	Language = 'language'
}
